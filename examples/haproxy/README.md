This directory contains a Docker image running a HAProxy instance that
uses `smtp-http-proxy` to post to Slack.

Full details can be found in 
[https://el-tramo.be/blog/haproxy-webhook-alerts/](this blog post).
